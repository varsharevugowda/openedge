#include<stdio.h>
int main()
{

    struct student{
    char id[15];
    char fname[20];
    char lname[20];
    char address[10];
    float tmarks[30];
    };

struct student std[3];

pts=std;

for(i=0;i<2;i++)
    printf("first:%s",)

scanf("%s",ptr->id)//store the value in correpondinf variable that is id



printf("result via ptr");
printf("td:",ptr->)
